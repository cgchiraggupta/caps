# HinglishCaps — Backend Dependencies
# Install: pip install -r requirements.txt

# FastAPI
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.12

# Caption engine (local package — must be in PYTHONPATH)
# batch.py is expected at the project root

# Runtime
pydantic>=2.0.0
